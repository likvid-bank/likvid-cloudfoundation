# Starterkit: **${project_display_name}**

## Details

| Property | Value |
|----------|-------|
| **meshProject** | `${project_identifier}` |
| **Landing Zone** | ${landing_zone} (`${landing_zone_name}`) |
| **STACKIT Project** | ${stackit_project} |
| **Network** | ${network} |
| **Project Admin** | ${project_admin} |

Your STACKIT project is managed through this meshProject. Order further building blocks inside it to
add storage buckets or a Git repository.
