output "documentation_md" {
  value = <<EOF
# Cloud Native Landing Zone

This landing zone is inteded for applications teams that want to work natively without AWS and can take
full responsibility for their own workloads.

This landing zone currently comes in two flavors `dev` and `prod` without any further restrictions enforced in them.
EOF
}
